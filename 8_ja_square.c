#include <stdio.h>
#define MAXN 10

int mat[MAXN][MAXN];
int vis[MAXN][MAXN];

void d

int main(int argc, char *argv[])
{
     for (int i = 0; i < N; ++i)
     {
	  scanf("%s", s);
	  for (int j = 0; j < N; ++j)
	       mat[i + 1][j + 1] = s[j] - '0';
     }
     return 0;
}
