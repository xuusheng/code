#include<iostream>
#include<fstream>
#include<cmath>
#include<ctime>
#include<cstring>
using namespace std;
int main(void)
{
     ifstream fin("in");
     ofstream fout("out");

     int i, j;
     char n[10]
     cin>>n;
     for (abc = 111; abc <= 999; ++abc)
	  for (de = 11; de <= 99; ++de)
	  {
	       x = abc * (de % 10);
	       y = abc * (de / 10);
	       z = abc * de;
	       sprintf(buf, "%d%d%d%d%d", abc, de, x, y, z);
	       ok = 1;
	       if (strchr(s, buf[i]) == NULL)
	  }
     

     return 0;
}
